import React from 'react';

/**
 * Header - titulo de Generador de QR
 * @returns
 */
const Header: React.FC = () => {
    return (
        <header className="header">
            <h1 className="title">Generador de Código QR</h1>
        </header>
    );
};

export default Header;